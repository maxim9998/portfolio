import NavBar from "./NavBar";

const Header = () => {
  return (
    <header className="w-full flex items-center justify-center p-6 fixed top-0 left-0">
      <NavBar />
    </header>
  );
};

export default Header;
